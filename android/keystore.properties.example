# Copy this file to keystore.properties and fill in your local release keystore values.
# keystore.properties is intentionally ignored by Git. Never commit signing passwords or a keystore.
storeFile=C:/path/to/clipbridge-release.jks
storePassword=change-me
keyAlias=clipbridge
keyPassword=change-me
